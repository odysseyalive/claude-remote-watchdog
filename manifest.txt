# claude-remote-watchdog install manifest
#
# The single authoritative list of every file the installers ship.
# Consumed by BOTH installers: install.sh (bash, Linux/macOS/WSL) and
# install.ps1 (PowerShell, Windows). Each downloads every file listed here
# from the repo and writes it under ~/.claude/. Add every new shipped file
# here — there are no per-installer file lists to keep in sync.
#
# Format: [flag] <source path relative to repo root> <dest path relative to ~/.claude>
#   (no flag)  fetch unconditionally, overwriting any existing copy
#   exec       fetch unconditionally; mark executable (chmod +x)
#   keep       fetch only if absent on the host (user-editable; never clobber)

exec remote-watchdog.sh scripts/remote-watchdog.sh
     remote-watchdog.md  commands/remote-watchdog.md
